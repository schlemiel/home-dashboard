# Optional: host port on your machine
DASHBOARD_PORT=8088

# Optional: attach the app to an existing Docker network
# Example: DASHBOARD_NETWORK=homelab
DASHBOARD_NETWORK=home-dashboard-net

# Optional: timezone for logs and app runtime
TZ=Europe/Berlin
